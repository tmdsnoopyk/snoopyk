//function -> action
// function block of code with name
// function -> subroutine (subprogram)
// function -> black box (engine)

// + DRY --> don;t repeat yourself
// + separation of concerns (separarea dupa sens)
// + SOLID

// ! decalare  + call (declarata si apelare)
// ! capsule
// ! parameters / arguments
// ! return values

function name ( arg1, arg2, arg3, ... ) {
    ins1;
    ins2;
    ...
    insn;
    return value;
}
